#include <stdio.h>
#include <stdlib.h>
#include "comp_list.h"

int main()
{
/*
	List* list = list_init();
	List* aux;

	list = list_insert(list, 5, "var1");
	list = list_insert(list, 1, "var2");
	list = list_insert(list, 8, "var3");
	list_print(list);

	List* nova = list_copy(nova,list);

	printf("->COPY TEST\n");

	list_print(nova);

	aux = list_lookup(list, "var2");
	printf("|LOOKUP TEST\n");
	printf("type = %d text = %s, value->next = %s\n", aux->type, aux->text, aux->next->text);

	list = list_remove(list,"var2");
	
	list_print(list);
	printf("\n");

	list = list_remove(list,"var1");
	list = list_remove(list,"var3");

	list_print(list);


	list_print(nova);*/
	return 0;

}
