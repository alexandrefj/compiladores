#include <stdio.h>
#include <stdlib.h>

#include "comp_graph.h"

int main()
{
	printf("\n");

	Graph* graph = NULL;
	/*createGraph(graph, 6);

	addEdge(graph, 2, 1);
	addEdge(graph, 1, 3);
	addEdge(graph, 1, 0);
	addEdge(graph, 1, 4);
	addEdge(graph, 3, 5);
	addEdge(graph, 3, 4);
	addEdge(graph, 0, 4);

	printGraph(graph);*/

	return 0;
}
